{% form_theme form 'form/formDivLayout.html.twig' %}
<div id="dialog" title="Nouvelle Personne">
    <div class="container ">
        <div class="row">
            <div class="col-xs-12">
                <br>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    <br>
</div>